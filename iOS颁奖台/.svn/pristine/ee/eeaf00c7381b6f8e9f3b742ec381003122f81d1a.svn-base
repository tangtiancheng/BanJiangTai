//
//  XPRegionEnity.m
//  XPApp
//
//  Created by xinpinghuang on 1/6/16.
//  Copyright © 2016 ShareMerge. All rights reserved.
//

#import "XPRegionEntity.h"

@implementation XPRegionEntity

//@property (nonatomic, assign) NSInteger id;
//@property (nonatomic, strong) NSString *name;
//@property (nonatomic, assign) NSInteger level;
//@property (nonatomic, assign) NSInteger parentId;
//@property (nonatomic, assign) NSInteger status;

- (NSString *)description
{
    return [NSString stringWithFormat:@"<id=%ld;name=%@;level=%ld;parentId=%ld;status=%ld>", (long)self.id, self.name, (long)self.level, (long)self.parentId, (long)self.status];
}

// Specify default values for properties

//+ (NSDictionary *)defaultPropertyValues
//{
//    return @{};
//}

// Specify properties to ignore (Realm won't persist these)

//+ (NSArray *)ignoredProperties
//{
//    return @[];
//}

@end
